name: Test No Permissions

on:
  workflow_call:
    inputs:
      message:
        required: false
        type: string
        default: "hello"
    secrets:
      LLM_API_KEY:
        required: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ inputs.message }}"
